DTOpenDist <- rename(DTOpenDist, c("Date"="Open"))
DTAnalysisDist <- rename(DTAnalysisDist, c("Date"="Analysis"))
DTBacklogDist <- rename(DTBacklogDist, c("Date"="Backlog"))
DTIPDist <- rename(DTIPDist, c("Date"="IP"))
DTResolvedDist <- rename(DTResolvedDist, c("Date"="Resolved"))
DTReleasingDist <- rename(DTReleasingDist, c("Date"="Releasing"))
DTClosedDist <- rename(DTClosedDist, c("Date"="Closed"))
DTDoneDist <- rename(DTDoneDist, c("Date"="Done"))

