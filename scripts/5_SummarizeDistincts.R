DTOpenDist$sums <- cumsum(DTOpenDist$count)
DTAnalysisDist$sums <- cumsum(DTAnalysisDist$count)
DTBacklogDist$sums <- cumsum(DTBacklogDist$count)
DTIPDist$sums <- cumsum(DTIPDist$count)
DTResolvedDist$sums <- cumsum(DTResolvedDist$count)
DTReleasingDist$sums <- cumsum(DTReleasingDist$count)
DTClosedDist$sums <- cumsum(DTClosedDist$count)
DTDoneDist$sums <- cumsum(DTDoneDist$count)



